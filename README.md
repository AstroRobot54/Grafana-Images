## Overview

This repo contains images of a KSAT ground station, which is queried by a Grafana dashboard for publicity purposes. The dashboard also queries a .html page from /satellite_visualizer, though just letting that sit right now.
